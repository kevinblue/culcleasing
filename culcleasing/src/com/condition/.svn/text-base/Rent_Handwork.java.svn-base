package com.condition;

import java.util.List;

/**
 * <p>租金手工调整处理类。</p>
 * @author 小谢
 * <p>Jun 11, 2010</p>
 */
public class Rent_Handwork {
	/**
	 * <p>租金测算手工调整数据操作。</p>
	 * @author 小谢
	 * @param args 参数 类型：List<String>
	 * @return 返回操作结果
	 */
	public int Rent_HW_Oper(List<String> args ){
		int flag = 0;
		String repeat_flag = "0";
		//接参数
		String stype = args.get(0);
		String temp = args.get(1);//判断是否是项目还是合同
		String	contract_id = args.get(2);
		String	proj_id = args.get(3);
		String	doc_id = args.get(4);
		String	rent_list = args.get(5);//期项
		String	plan_date = args.get(6);//状态
		String	rent = args.get(7);//租金
		String	corpus = args.get(8);//本金
		String	interest = args.get(9);//利息
		String	plan_status = args.get(10);//回笼状态
		//
		
		return flag;
	}
}
